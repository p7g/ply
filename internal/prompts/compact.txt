For this compaction request only, summarize the context for continuation. Do not use tools or produce or revise a plan, even if the active turn is in plan mode. Preserve the outstanding user request, in-progress work, task IDs, important paths, decisions, and unresolved problems. The plan is preserved separately verbatim. Return only the summary. The active turn's mode will resume after compaction.
